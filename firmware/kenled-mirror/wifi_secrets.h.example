// Copy to wifi_secrets.h (gitignored — the repo is public) and fill in.
#pragma once
#define WIFI_SSID "your-network"
#define WIFI_PASS "your-password"

// Optional: a GitHub personal access token raises the API poll quota from
// 60/hr (shared per IP) to 5000/hr. A fine-grained PAT with NO permissions
// (public read-only) is all it needs — never use a token with write access,
// since the device's TLS is unverified.
// #define GH_TOKEN "github_pat_..."
